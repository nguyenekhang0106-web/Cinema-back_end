server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    # Tham s? createDatabaseIfNotExist=true s? ra l?nh cho MySQL t? t?o database n?u n� ch?a t?n t?i
    url: "jdbc:mysql://localhost:3308/cinema_db?createDatabaseIfNotExist=true"
    username: root
    password: root
  jpa:
    hibernate:
      format_sql: true
      #ddl-auto: update
      ddl-auto: create # dùng khi update tạo lại bảng từ đầu
    show-sql: true